package com.alyssanicoll.view{	import com.alyssanicoll.events.ScrollBarEvent;		import flash.display.*;	import flash.events.*;		import libs.ScrollerBase;
		public class ScrollBar extends ScrollerBase	{		private var yOffset:Number;		private var yMin:Number;		private var yMax:Number;		private var theMouseDown:Boolean;				public function ScrollBar(stage:Stage):void		{			yMin = 0;			yMax = track.height - thumb.height;			thumb.addEventListener(MouseEvent.MOUSE_DOWN, thumbDown);			thumb.addEventListener(MouseEvent.MOUSE_UP, thumbUp);			stage.addEventListener(MouseEvent.MOUSE_UP, thumbUp);		}				private function thumbDown(e:MouseEvent):void		{			stage.addEventListener(MouseEvent.MOUSE_MOVE, thumbMove);			yOffset = mouseY - thumb.y;			theMouseDown = true;		}		private function thumbUp(e:MouseEvent):void		{			if(theMouseDown) {				stage.removeEventListener(MouseEvent.MOUSE_MOVE, thumbMove);				theMouseDown = false;			}		}		private function thumbMove(e:MouseEvent):void		{			thumb.y = mouseY - yOffset;			if(thumb.y <= yMin)				thumb.y = yMin;			if(thumb.y >= yMax)				thumb.y = yMax;			dispatchEvent(new ScrollBarEvent(thumb.y / yMax));			e.updateAfterEvent();		}	}}